<?xml version="1.0" encoding="utf-8"?>
<!-- Same ground as the pre-21 drawable: the app's own deep green, so the
     launch window matches the first frame instead of flashing the
     platform's white. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />
</layer-list>
