# `manual` marks the visual-QA screenshot helper: it software-rasterizes
# every painter, which is far too slow for the default suite. Run it
# deliberately with `flutter test --tags=manual`.
tags:
  manual:
    skip: "visual-QA only — run with `flutter test --tags=manual`"
