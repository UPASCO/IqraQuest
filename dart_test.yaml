# `manual` marks the visual-QA screenshot helper: it writes PNGs to
# build/screenshots/ for human review rather than asserting behaviour.
# It runs in the default suite (it is fast), and CI can drop it with
# `flutter test --exclude-tags=manual`.
tags:
  manual:
